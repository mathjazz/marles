const p = document.getElementById('progress'); addEventListener('scroll', () => { const h = document.documentElement; p.style.width = (h.scrollTop / (h.scrollHeight - h.clientHeight) * 100) + '%' }); const obs = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible') }), { threshold: .12 }); document.querySelectorAll('.block,.reveal,.card,.photo,figure').forEach(e => { e.classList.add('reveal'); obs.observe(e) }); const lb = document.getElementById('lightbox'), img = lb.querySelector('img'), cap = lb.querySelector('p'); document.querySelectorAll('figure img').forEach(i => i.onclick = () => { img.src = i.src; cap.textContent = i.closest('figure').querySelector('figcaption')?.textContent || ''; lb.classList.add('open') }); lb.querySelector('button').onclick = () => lb.classList.remove('open'); lb.onclick = e => { if (e.target === lb) lb.classList.remove('open') }; document.onkeydown = e => { if (e.key === 'Escape') lb.classList.remove('open') };